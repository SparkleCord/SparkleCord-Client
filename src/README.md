### To start SparkleCord, Open `index.html`.
Please note that in Android 10+, you cannot use the local version.  
This is due to a feature known as scoped storage, which makes it way harder to access local files.  

**Use the web version if you are experiencing problems on Android.**